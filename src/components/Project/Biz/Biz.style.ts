import styled from "styled-components";

export const StyledBiz = styled.section`
  padding: 48px 0 80px 0;
  border-bottom: 1px solid #ebebeb;
`;

export const Inner = styled.div`
  width: 1200px;
  margin: 0 auto;
  position: relative;
  box-sizing: border-box;

  h2 {
    font-size: 24px;
    margin: 0 0 16px 0;
  }

  p {
    font-size: 16px;
  }
`;
