import styled from "styled-components";

export const StyledOutline = styled.section`
  padding: 48px 0 60px 0;
`;

export const Inner = styled.div`
  width: 1200px;
  margin: 0 auto;
  position: relative;
  box-sizing: border-box;

  border-bottom: 1px solid #ebebeb;
  padding: 0 0 40px 0;

  h2 {
    font-size: 24px;
    margin: 0 0 16px 0;
  }

  p {
    font-size: 16px;
  }
`;
